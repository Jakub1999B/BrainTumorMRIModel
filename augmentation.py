# -*- coding: utf-8 -*-
"""augmentation.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1qI30LdQPeoR-4k4-OOI1bHTpXuUr6_9V
"""

import os
import sys
sys.path.append("/content/drive/MyDrive/BrainTumorMRIModel")
import numpy as np
import pandas as pd
import tensorflow as tf
import matplotlib.pyplot as plt
from tensorflow import keras
from skimage import color
from skimage import io
import sklearn
from tqdm import tqdm
import cv2
import seaborn as sns
from keras import layers
from sklearn.model_selection import train_test_split
from tensorflow.keras.preprocessing.image import ImageDataGenerator
from tensorflow.keras import datasets, layers, models
from sklearn.metrics import confusion_matrix
from sklearn.metrics import classification_report
import argparse
from tensorflow.keras.utils import to_categorical
from imutils import paths
from tensorflow.keras.optimizers import SGD
from keras import Sequential
from PIL import Image

def random_invert_img(x, p=0.5):
  if  tf.random.uniform([]) < p:
    x = (255-x)
  else:
    x
  return x

aug1 = ImageDataGenerator(
    rotation_range=90,
    horizontal_flip = True,
    vertical_flip = True,
    fill_mode = 'nearest'
)

aug2 = ImageDataGenerator(
    preprocessing_function=random_invert_img
)

aug3 = ImageDataGenerator(
    channel_shift_range = 0.4
)

aug4 = ImageDataGenerator(
    rotation_range=90,
    horizontal_flip = True,
    vertical_flip = True,
    preprocessing_function=random_invert_img
)

aug5 = ImageDataGenerator(
    rotation_range=90,
    horizontal_flip = True,
    vertical_flip = True,
    channel_shift_range = 0.4
)

def aug(img, gen):
  image = np.expand_dims(img, axis=0)
  image = gen.flow(image)
  image = image[0]
  image = image[0,:,:,0]

  return image

def viz_aug(path):
  image = Image.open(path)
  image = np.asarray(image)
  image = image / 255.

  au1 = aug(image, aug1)
  au2 = aug(image, aug2)
  au3 = aug(image, aug3)
  au4 = aug(image, aug4)
  au5 = aug(image, aug5)

  plt.figure(figsize = (20,20), dpi=300)
  plt.subplot(2,3,1)
  plt.axis(False)
  plt.imshow(image, cmap='gray')
  plt.title('Original')
  plt.subplot(2,3,2)
  plt.axis(False)
  plt.imshow(au1, cmap='gray')
  plt.title('Augmentation 1')
  plt.subplot(2,3,3)
  plt.axis(False)
  plt.imshow(au2, cmap='gray')
  plt.title('Augmentation 2')
  plt.subplot(2,3,4)
  plt.axis(False)
  plt.imshow(au3, cmap='gray')
  plt.title('Augmentation 3')
  plt.subplot(2,3,5)
  plt.axis(False)
  plt.imshow(au4, cmap='gray')
  plt.title('Augmentation 4')
  plt.subplot(2,3,6)
  plt.imshow(au5, cmap='gray')
  plt.axis(False)
  plt.title('Augmentation 5')
  plt.tight_layout()